import 'package:flutter/material.dart';

/// Croatian-inspired CroLingo color palette.
abstract final class AppColors {
  /// Primary Adriatic blue.
  static const primary = Color(0xFF1769D2);

  /// Darker primary used for tactile depth.
  static const primaryPressed = Color(0xFF1052A8);

  /// Restrained Croatian red accent.
  static const accent = Color(0xFFE53935);

  /// Warm crown and reward gold.
  static const crown = Color(0xFFF4B942);

  /// Main app background.
  static const background = Color(0xFFF5F8FC);

  /// Selected informational surface.
  static const selectedSurface = Color(0xFFE8F1FC);

  /// Primary text and crow charcoal.
  static const charcoal = Color(0xFF26343D);

  /// Secondary text.
  static const slate = Color(0xFF536475);

  /// Subtle borders.
  static const border = Color(0xFFD9E2EC);

  /// Correct-answer green.
  static const success = Color(0xFF2DAA63);

  /// Incorrect-answer red.
  static const error = Color(0xFFC92A35);
}
